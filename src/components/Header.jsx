import s from './Header.module.css'

const Header = () => {
    return (
      <header className={s.header}>
        <img src="/images/test.jpg" alt="fail" />
      </header>
    );
}

export default Header;